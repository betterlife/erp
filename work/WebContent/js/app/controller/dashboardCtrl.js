var dashboardCtrl = function ($scope, $http, loginService, $location) {
    "use strict";
};

dashboardCtrl.$inject = ['$scope', '$http', 'loginService', '$location'];

angular.module('mainApp').controller('dashboardCtrl', dashboardCtrl);
